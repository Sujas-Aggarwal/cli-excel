#pragma once;
#include <iostream>
#include <vector>
#include <sheet.h>
using namespace std;
class Actions{
public:
    Actions() {}
    ~Actions() {}
    // static void setCell(Sheet &sheet,int row, int col, const int &value);
    // static int getCell(Sheet &sheet,int row, int col);
};